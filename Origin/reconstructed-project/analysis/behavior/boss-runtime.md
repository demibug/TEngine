# Boss runtime

BossBase extends EnemyBase, uses the `boss` visual pool, EnemyData boss multipliers, path movement, spatial registration, damage/death/reward and SkillManager cooldown activation. All twelve factory keys map to distinct classes and skill definitions. Detailed per-boss animation/VFX timelines are deferred.
