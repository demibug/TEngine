# 弓武器 Buff 集成

- 长弓：装备时 `attRange(2) +0.5`，卸下时按 Buff ID 移除。
- 角弓：创建永久 `attSpeed(1)` 层；连续攻击同一目标每次把乘法值增加 `0.05`，上限 `20`；切换目标重置为 0。
- 神臂弓：同角弓，但每次增加 `0.15`，上限 `20`。
- 落日弓：装备时应用乘法 `attRange(2) ×1` 墭量（即额外一倍基础范围），卸下移除。其凤凰投射物行为仍单独标记为部分恢复。

`WeaponBuffPort` 已从暂缓占位变为严格 BuffManager 适配器，不再直接修改 owner 属性。
